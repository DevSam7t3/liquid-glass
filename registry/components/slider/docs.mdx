---
title: Liquid Slider
description: A physics-based liquid glass range slider with refractive bezel effects, specular highlights, and smooth drag interactions.
---

import { TypeTable } from 'fumadocs-ui/components/type-table';
import { Tab, Tabs } from 'fumadocs-ui/components/tabs';
import { Step, Steps } from 'fumadocs-ui/components/steps';

## Preview

<Tabs items={['Preview', 'Code']}>
  <Tab value="Preview">
    <LiquidSliderDemo />
  </Tab>
  <Tab value="Code">
    ```tsx
    'use client';

    import { useState } from 'react';
    import { LiquidSlider } from '@/components/ui/liquid-slider';

    export default function Demo() {
      const [value, setValue] = useState(50);

      return (
        <LiquidSlider
          min={0}
          max={100}
          value={value}
          className="w-full"
          events={{ input: ({ value }) => setValue(value) }}
        />
      );
    }
    ```
  </Tab>
</Tabs>

---

## Installation

<Steps>
  <Step>
    ### Install the dependency

    `LiquidSlider` is powered by the `@avenra/liquid-glass` package.

    <Tabs items={['npm', 'pnpm', 'yarn', 'bun']}>
      <Tab value="npm">
        ```bash
        npm install @avenra/liquid-glass
        ```
      </Tab>
      <Tab value="pnpm">
        ```bash
        pnpm add @avenra/liquid-glass
        ```
      </Tab>
      <Tab value="yarn">
        ```bash
        yarn add @avenra/liquid-glass
        ```
      </Tab>
      <Tab value="bun">
        ```bash
        bun add @avenra/liquid-glass
        ```
      </Tab>
    </Tabs>
  </Step>

  <Step>
    ### Add the component via shadcn CLI

    <Tabs items={['npm', 'pnpm', 'yarn', 'bun']}>
      <Tab value="npm">
        ```bash
        npx shadcn@latest add https://avenra.online/r/liquid-slider.json
        ```
      </Tab>
      <Tab value="pnpm">
        ```bash
        pnpm dlx shadcn@latest add https://avenra.online/r/liquid-slider.json
        ```
      </Tab>
      <Tab value="yarn">
        ```bash
        yarn dlx shadcn@latest add https://avenra.online/r/liquid-slider.json
        ```
      </Tab>
      <Tab value="bun">
        ```bash
        bunx shadcn@latest add https://avenra.online/r/liquid-slider.json
        ```
      </Tab>
    </Tabs>

    This places the component at `components/ui/liquid-slider.tsx`.
  </Step>

  <Step>
    ### Manual installation

    Create `components/ui/liquid-slider.tsx` with the following content:

    ```tsx title="components/ui/liquid-slider.tsx"
    'use client';

    import React, { useEffect, useRef, forwardRef, useImperativeHandle } from 'react';
    import {
      createLiquidSlider,
      LiquidSliderHandle,
      LiquidSliderEventMap,
      LiquidSliderOptions,
    } from '@avenra/liquid-glass';

    export interface LiquidSliderProps extends Omit<React.HTMLAttributes<HTMLDivElement>, 'onChange'> {
      min: number;
      max: number;
      value: number;
      options?: LiquidSliderOptions;
      events?: {
        [K in keyof LiquidSliderEventMap]?: (payload: LiquidSliderEventMap[K]) => void;
      };
    }

    export const LiquidSlider = forwardRef<HTMLDivElement, LiquidSliderProps>(
      ({ min, max, value, options, events, ...props }, ref) => {
        const containerRef = useRef<HTMLDivElement>(null);
        const handleRef = useRef<LiquidSliderHandle | null>(null);

        useImperativeHandle(ref, () => containerRef.current as HTMLDivElement);

        useEffect(() => {
          if (!containerRef.current) return;

          if (containerRef.current.children.length > 0) return; // already initialized

          handleRef.current = createLiquidSlider(containerRef.current, {
            min, max, value, blur: 0, ...options,
          });
          return () => { handleRef.current?.destroy(); handleRef.current = null; };
        }, []);

        useEffect(() => {
          if (!handleRef.current) return;
          handleRef.current.value = value;
        }, [value]);

        useEffect(() => {
          if (!handleRef.current) return;
          handleRef.current.value = Math.min(Math.max(value, min), max);
        }, [min, max]);

        useEffect(() => {
          if (!handleRef.current || !events) return;
          const handle = handleRef.current;
          for (const key in events) {
            const event = key as keyof LiquidSliderEventMap;
            const handler = events[event];
            if (handler) handle.on(event, handler as (payload: LiquidSliderEventMap[typeof event]) => void);
          }
          return () => {
            if (!handleRef.current || !events) return;
            for (const key in events) {
              const event = key as keyof LiquidSliderEventMap;
              const handler = events[event];
              if (handler) handleRef.current.off(event, handler as (payload: LiquidSliderEventMap[typeof event]) => void);
            }
          };
        }, [events]);

        return <div ref={containerRef} {...props} />;
      },
    );

    LiquidSlider.displayName = 'LiquidSlider';
    ```
  </Step>
</Steps>

---

## Usage

```tsx
import { LiquidSlider } from '@/components/ui/liquid-slider';
```

```tsx
'use client';

import { useState } from 'react';
import { LiquidSlider } from '@/components/ui/liquid-slider';

export default function Example() {
  const [value, setValue] = useState(50);

  return (
    <LiquidSlider
      min={0}
      max={100}
      value={value}
      className="w-full"
      events={{ input: ({ value }) => setValue(value) }}
    />
  );
}
```

> `LiquidSlider` is a **controlled component** — you must supply a `value` prop and update it via the `events.input` handler. The component will not update its position without an external state change.

---

## Examples

### Controlled with external buttons

Drive the slider value from outside the component using React state. The glass handle position updates reactively whenever `value` changes.

<Tabs items={['Preview', 'Code']}>
  <Tab value="Preview">
    <LiquidSliderControlledDemo />
  </Tab>
  <Tab value="Code">
    ```tsx
    'use client';

    import { useState } from 'react';
    import { LiquidSlider } from '@/components/ui/liquid-slider';

    export default function Example() {
      const [value, setValue] = useState(30);

      return (
        <div className="flex flex-col gap-4">
          <LiquidSlider
            min={0}
            max={100}
            value={value}
            className="w-full"
            events={{ input: ({ value }) => setValue(value) }}
          />
          <div className="flex items-center gap-3">
            <button onClick={() => setValue((v) => Math.max(0, v - 10))}>−10</button>
            <span>{value}</span>
            <button onClick={() => setValue((v) => Math.min(100, v + 10))}>+10</button>
          </div>
        </div>
      );
    }
    ```
  </Tab>
</Tabs>

---

### Step snapping

Use the `step` option inside `options` to snap the thumb to discrete intervals.

<Tabs items={['Preview', 'Code']}>
  <Tab value="Preview">
    <LiquidSliderStepDemo />
  </Tab>
  <Tab value="Code">
    ```tsx
    <LiquidSlider
      min={0}
      max={100}
      value={value}
      options={{ step: 25 }}
      className="w-full"
      events={{ input: ({ value }) => setValue(value) }}
    />
    ```
  </Tab>
</Tabs>

---

### Custom range

`min` and `max` accept any numeric range — negative values are supported. The slider clamps the current value automatically when the range changes.

<Tabs items={['Preview', 'Code']}>
  <Tab value="Preview">
    <LiquidSliderRangeDemo />
  </Tab>
  <Tab value="Code">
    ```tsx
    <LiquidSlider
      min={-20}
      max={50}
      value={temp}
      className="w-full"
      events={{ input: ({ value }) => setTemp(value) }}
    />
    ```
  </Tab>
</Tabs>

---

### input vs change events

`input` fires continuously while dragging (like `oninput` on a native range). `change` fires once on pointer-up (like `onchange`). Use `change` for expensive operations such as API calls.

<Tabs items={['Preview', 'Code']}>
  <Tab value="Preview">
    <LiquidSliderEventsDemo />
  </Tab>
  <Tab value="Code">
    ```tsx
    <LiquidSlider
      min={0}
      max={100}
      value={live}
      className="w-full"
      events={{
        input:  ({ value }) => setLive(value),      // every frame
        change: ({ value }) => commitToServer(value), // pointer-up only
      }}
    />
    ```
  </Tab>
</Tabs>

---

### Forwarded ref

`LiquidSlider` forwards its ref to the underlying `<div>` container, giving you direct DOM access.

```tsx
'use client';

import { useRef } from 'react';
import { LiquidSlider } from '@/components/ui/liquid-slider';

export default function Example() {
  const sliderRef = useRef<HTMLDivElement>(null);

  return (
    <LiquidSlider
      ref={sliderRef}
      min={0}
      max={100}
      value={50}
      className="w-full"
    />
  );
}
```

---

### Glass appearance options

All `GlassOptions` from the shared engine are available via the `options` prop — the same knobs exposed by `LiquidButton`.

```tsx
<LiquidSlider
  min={0}
  max={100}
  value={value}
  className="w-full"
  options={{
    bezelWidth: 12,
    refractiveIndex: 1.8,
    specularSlope: 1,
    saturation: 1.6,
    profile: 'convexSquircle',
  }}
  events={{ input: ({ value }) => setValue(value) }}
/>
```

---

## API Reference

### LiquidSliderProps

<TypeTable
  type={{
    min: {
      description: 'Minimum value of the slider range.',
      type: 'number',
      required: true,
    },
    max: {
      description: 'Maximum value of the slider range.',
      type: 'number',
      required: true,
    },
    value: {
      description: 'Current controlled value. Must be kept in sync via the events.input handler.',
      type: 'number',
      required: true,
    },
    options: {
      description: 'Slider and glass effect configuration. See LiquidSliderOptions below.',
      type: 'LiquidSliderOptions',
    },
    events: {
      description: 'Typed event handlers bound to the LiquidSlider instance. Supports: input, change, destroy.',
      type: 'LiquidSliderEvents',
    },
  }}
/>

All standard `HTMLDivElement` attributes (`className`, `style`, `aria-*`, etc.) are forwarded via rest props. `onChange` is intentionally excluded — use `events.change` instead.

---

### LiquidSliderOptions

`LiquidSliderOptions` extends `GlassOptions` with slider-specific fields.

<TypeTable
  type={{
    min: {
      description: 'Minimum value. Prefer the top-level min prop for reactive updates.',
      type: 'number',
      default: '0',
    },
    max: {
      description: 'Maximum value. Prefer the top-level max prop for reactive updates.',
      type: 'number',
      default: '100',
    },
    value: {
      description: 'Initial value. Prefer the top-level value prop for controlled usage.',
      type: 'number',
      default: '0',
    },
    step: {
      description: 'Snap interval. The thumb locks to multiples of this value within the range.',
      type: 'number',
    },
    width: {
      description: 'Override element width in px. Auto-detected from the DOM if omitted.',
      type: 'number',
    },
    height: {
      description: 'Override element height in px. Auto-detected from the DOM if omitted.',
      type: 'number',
    },
    radius: {
      description: 'Corner radius in px. Pass null to auto-read from CSS border-radius.',
      type: 'number | null',
    },
    bezelWidth: {
      description: 'Width of the refractive bezel rim in px.',
      type: 'number',
      default: '20',
    },
    glassThickness: {
      description: 'Virtual glass depth that controls lateral ray drift through the lens.',
      type: 'number',
      default: '80',
    },
    refractiveIndex: {
      description: 'Index of refraction n₂. Air = 1.0, typical glass ≈ 1.5. Higher values produce stronger distortion.',
      type: 'number',
      default: '1.5',
    },
    profile: {
      description: 'Surface height profile of the bezel rim, controlling how light bends across it.',
      type: "'convexSquircle' | 'flat' | 'concave' | Profile",
      default: "'convexSquircle'",
    },
    blur: {
      description: 'Pre-displacement Gaussian blur stdDeviation. Defaults to 0 for sliders (sharper track edges).',
      type: 'number',
      default: '0',
    },
    saturation: {
      description: 'Post-displacement colour saturation multiplier.',
      type: 'number',
      default: '1.3',
    },
    specularSlope: {
      description: 'Specular highlight intensity on the glass surface. Range: 0 (none) to 1 (full).',
      type: 'number',
      default: '0.8',
    },
    filterMode: {
      description: 'SVG filter blend mode used when compositing the specular highlight layer.',
      type: "'screen' | 'overlay' | FilterMode",
      default: "'screen'",
    },
  }}
/>

---

### LiquidSliderEventMap

<TypeTable
  type={{
    input: {
      description: 'Fires continuously while the thumb is being dragged. Equivalent to the native oninput event on a range input.',
      type: '{ value: number; element: Element }',
    },
    change: {
      description: 'Fires once when the pointer is released. Equivalent to the native onchange event on a range input. Prefer this for expensive side-effects like API calls.',
      type: '{ value: number; element: Element }',
    },
    destroy: {
      description: 'Fires when the LiquidSlider instance is destroyed on component unmount.',
      type: 'undefined',
    },
  }}
/>
